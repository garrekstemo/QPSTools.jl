Manifest.toml
!bootstrap/data/
figures/
docs/build/
.DS_Store
*.pdf

# Measurement data lives outside the repo, under ~/Data (see CLAUDE.md).
# Kept as a guard so a stray data/ folder never gets committed again.
/data/
